{
  "name": "supermarket-billing-software",
  "version": "1.0.0",
  "description": "Complete supermarket billing/POS software — dashboard, products, billing, customers, inventory, reports, auth.",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "migrate": "npm run migrate --prefix backend",
    "test": "npm test --prefix backend",
    "start": "npm start --prefix backend",
    "setup": "npm run install:all && npm run build:frontend && npm run migrate",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
